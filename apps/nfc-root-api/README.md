# nfc-root-api

This Application was generated with [@nx-bun/nx](https://github.com/jordan-hall/nx-bun)

## Building

Run `nx build nfc-root-api` to build the app.

## Running unit serve

Run `nx serve nfc-root-api` to execute the unit tests via [bun test](https://bun.sh/docs/cli/test).

## Running unit tests

Run `nx test nfc-root-api` to execute the unit tests via [bun test](https://bun.sh/docs/cli/test).
