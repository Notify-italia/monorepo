# Modules

The _modules_ folder contains a series of componets/services/scss files, grouped in folders, that usually all correlate to a single feature or function.

The newly created modules are only thematically connected, all components are standalone and all services aren't provided by a module but the folder also contains some legacy Angular Modules
